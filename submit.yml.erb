---
batch_connect:
  template: "basic"
  conn_params:
    - csrf_token
script:
  job_name: "ood-rstudio"
  native:
    - <%= "--nodes=1" %>
    - <%= "--ntasks=#{num_cores}" %>
    - <%= "--nodelist=#{node}" %>
